values:
  full:
    fn::rotate:
      rotator: swap
      inputs: {}
      state:
        a: bar1
        b: bar2
  short:
    fn::rotate::swap:
      state:
        a: bar1
        b: bar2

  reference:
    test: ${full.a} ${short.b}