values:
  inline: ${imports.foo.some_value}

  # any inline import is essentially treated as if it were in the imports list with {merge:false}
  # so they should be visible if you interpolate the whole "imports" object
  caveat: ${imports}
