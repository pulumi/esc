values:
  # Missing schema
  missing-schema:
    fn::conform:
      value: "test"

  # Missing value
  missing-value:
    fn::conform:
      schema: { type: string }

  # Not an object
  not-object:
    fn::conform: "invalid"
