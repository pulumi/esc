imports:
  - a: {rotateOnly: true, merge: false}

values:
  example:
    fn::rotate::echo:
      inputs:
        next: ${imports["a"].some_secret}
        caps: ${imports["a"].some_condition} # why isn't this being treated as missing during open???
      state:
        current: "foo"
        previous: "bar"
