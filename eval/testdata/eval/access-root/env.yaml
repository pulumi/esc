imports:
  - foo/bar

values:
  foo: "hello!"

  imp: ${.imports}
  ctx: ${.context}
  env: ${.environments}
  env2: ${.environments.foo.bar.some_value}
  values: ${.values}
  values2: ${.values.foo}
  invalid: ${.invalid}
