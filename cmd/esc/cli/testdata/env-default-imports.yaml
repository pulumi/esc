run: esc env open
process:
  fs:
    proj/.esc.yaml: |
      environment:
        organization: test-user
        imports:
          - default/the-env
  cwd: proj
environments:
  test-user/default/the-env:
    values:
      foo: bar

---
> esc env open
{
  "foo": "bar"
}

---
> esc env open
