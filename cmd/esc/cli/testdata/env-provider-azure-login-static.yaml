run: |
  esc env init default/test
  esc env provider azure-login static default/test cid tid /subscriptions/sub shhhhhh && esc env get default/test --definition

---
> esc env init default/test
Environment created: test-user/default/test
> esc env provider azure-login static default/test cid tid /subscriptions/sub shhhhhh
Provider updated.
> esc env get default/test --definition
values:
  azure:
    login:
      fn::open::azure-login:
        clientId: cid
        tenantId: tid
        subscriptionId: /subscriptions/sub
        clientSecret:
          fn::secret:
            ciphertext: ZXNjeAAAAAHz6Ojo6OjoVtjhwQ==

---
> esc env init default/test
> esc env provider azure-login static default/test cid tid /subscriptions/sub shhhhhh
> esc env get default/test --definition
