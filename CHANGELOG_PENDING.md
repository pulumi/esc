### Improvements

- Add support for `fn::toYAML` built-in function that encodes a value as YAML.
  [#642](https://github.com/pulumi/esc/pulls/642)
- Add `OpenYAMLOption` with `EnvironmentOverrides` to `client.OpenYAMLEnvironment`, allowing callers to
  substitute imported environments while resolving the import graph.

### Bug Fixes

### Breaking changes
